services:
  app:
    build: .
    container_name: verbex-metabase-app
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - VERBEX_API_KEY=${VERBEX_API_KEY}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_SCHEMA=${DB_SCHEMA}
    volumes:
      - ./app.py:/app/app.py
    restart: unless-stopped
    command: uvicorn app:app --host 0.0.0.0 --port 8000 --reload

#   metabase:
#     image: metabase/metabase:latest
#     container_name: metabase
#     hostname: metabase
#     volumes:
#       - /dev/urandom:/dev/random:ro
#     ports:
#       - 3500:3000
#     environment:
#       MB_DB_TYPE: postgres
#       MB_DB_DBNAME: metabaseappdb
#       MB_DB_PORT: 5432
#       MB_DB_USER: metabase
#       MB_DB_PASS: 123456
#       MB_DB_HOST: postgres
#     networks:
#       - metanet1
#     healthcheck:
#       test: curl --fail -I http://localhost:3000/api/health || exit 1
#       interval: 15s
#       timeout: 5s
#       retries: 5
#     depends_on:
#       - postgres

#   postgres:
#     image: postgres:latest
#     container_name: postgres
#     hostname: postgres
#     ports:
#       - 5432:5432
#     environment:
#       POSTGRES_USER: metabase
#       POSTGRES_DB: metabaseappdb
#       POSTGRES_PASSWORD: 123456
#     networks:
#       - metanet1

# networks:
#   metanet1:
#     driver: bridge
